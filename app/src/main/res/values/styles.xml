<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/verde</item>
        <item name="colorPrimaryDark">@color/verde_ifam</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/verde_ifam</item>
    </style>

    <style name="AppTheme.h1_black">
        <item name="android:textColor">@color/preto</item>
        <item name="android:textSize">25sp</item>
        <item name="android:fontFamily">@font/cabin_bold</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="AppTheme.lbl_black">
        <item name="android:textColor">@color/preto</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/cabin_bold</item>
    </style>

    <style name="AppTheme.txt">
        <item name="android:textColor">@color/preto</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/cabin</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
